import { connect } from 'react-redux';
import { setActivity, setAddresses } from '../actions';
//import reducers

//GenerateSearch

<<<<<<< HEAD
// const mapStateToProps = state => (
//map state properties to reducers
// );

// const mapDispatchToProps = dispatch => (
//map dispatch action to a prop in components
// );
=======
const mapStateToProps = state => {
  //map state properties to reducers
};

const mapDispatchToProps = dispatch => {
  return {
    //map dispatch action to a prop in components
  }
};
>>>>>>> Create react and redux skeleton

/* const GenerateSearch = connect(
  mapStateToProps,
  mapDispatchToProps
)(<mapped dispatch action property>) */

export default GenerateSearch;